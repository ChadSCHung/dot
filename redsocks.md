# Setting of redsocks5
Redsocks5 is a tool that redirect TCP packets to assigned SOCKS5 proxy.
I use it to connect with the server in company.

## Path
redsocks.conf -> /etc/redsocks.conf
redsocks.service -> /etc/systemd/system/redsocks.service
redsocks_iptables.sh -> put in $PATH
